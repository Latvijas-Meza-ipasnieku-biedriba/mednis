export interface PhotoReference {
    /**
     * Path to photo on the filesystem
     */
    path: string;

    /**
     * Path to photo on the filesystem that can be used as a src for images
     */
    webPath: string;
}
